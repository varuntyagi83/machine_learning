# Monte Carlo Data Observability Setup
# Example: Define data observability metrics and set up anomaly detection
mc_data_observability_config = {
    'table': 'SHIPMENTS',
    'columns': ['CustomerID', 'ProductID', 'TransportationMode', 'ShipmentDate'],
    'thresholds': {
        'accuracy': accuracy_threshold,
        'completeness': completeness_threshold,
        'consistency': consistency_threshold,
        'timeliness': timeliness_threshold
    }
}

# Set up Monte Carlo for data observability
monte_carlo.setup(mc_data_observability_config)
